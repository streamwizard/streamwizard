{
  "name": "@repo/ingest-control",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "doppler run --config dev_ingest_control -- bun --preload ./src/sentry.ts --hot src/index.ts",
    "start": "bun --preload ./src/sentry.ts src/index.ts",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/metrics": "workspace:*",
    "@repo/sentry": "workspace:*",
    "@repo/supabase": "workspace:*",
    "@repo/types": "workspace:*",
    "@sentry/bun": "^10",
    "@sentry/hono": "^10",
    "@supabase/supabase-js": "^2.105.1",
    "hono": "^4.12.21",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "@repo/typescript-config": "workspace:*",
    "typescript": "5.9.2"
  }
}
