{
  "name": "@repo/ws-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "doppler run --config dev_ws_server -- bun run --hot src/index.ts",
    "start": "bun run ./src/index.ts",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/metrics": "workspace:*",
    "@repo/supabase": "workspace:*",
    "@repo/types": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "^1.3.6",
    "typescript": "5.9.2"
  }
}
