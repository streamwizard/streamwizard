# Docs images

Screenshots referenced by the docs pages. Drop the files in at these exact paths
and the `<Frame>` placeholders will render them. Keep PNGs reasonably
compressed (aim < ~300 KB each).

## onboarding/
- `connect-twitch.png` — the Connect Twitch / login screen
- `setup-modal.png` — the six-step setup window (welcome step is a good frame)
- `sync-clips.png` — the "Pull in your clips" step

## clips/
- `clips-overview.png` — full clips page: filter bar + folders sidebar
- `filters-panel.png` — the expanded advanced filters panel
- `folders-sidebar.png` — the Folders tree in the sidebar (hover a row to show + and ⋮)
- `add-to-folder.png` — a clip card's ⋯ menu open on "Add to folder" (ideally showing the waterfall submenu)

## dashboard/
- `overview.png` — the Stream Analytics dashboard with stats, chart, and feed
